# 使用Whisper-Crawl

## 0. Install Pixi
- https://pixi.prefix.dev/latest/installation/

## 1. Run Whisper-Crawl with specified json filename

```bash
pixi run python batch_transcribe_all_videos.py --filename "中天新聞_videos.json"
```
